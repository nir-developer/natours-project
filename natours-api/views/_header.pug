header.header
  nav.nav.nav--tours
    a.nav__el(href='/') All tours
  .header__logo
    img(src='/img/logo-white.png' alt='Natours logo')
  nav.nav.nav--user
    if user
      a.nav__el.nav__el--logout Log out
      a.nav__el(href='/me')
        img.nav__user-img(src=`/img/users/${user.photo}` alt=`Photo of ${user.name}`)
        span= user.name.split(' ')[0]
    else
      a.nav__el(href='/login') Log in
      a.nav__el.nav__el--cta(href='#') Sign up
//- ///////MY CODE
//- header.header
//-     nav.nav.nav--tours
//-         a.nav__el(href='/') All tours
//-     .header__logo
//-         img(src='/img/logo-white.png' alt='Natours logo')
//-     nav.nav.nav--user
//-         //- THE user passed as property by the isLoggedIn M.W - res.locals.user = currentUser
//-         //- (if there was a cookie that contains the  JWT & User exists & Password has not been changed recently)
//-         if user
//-             a.nav__el.nav__el--logout Log out
//-             a.nav__el(href='/' )
//-                 img.nav__user-img(src=`/img/users/${user.photo}` alt=`Photo of ${user.name}`)
//-                 span= user.name.split(' ')[0]
//-         else
//-             a.nav__el(href='/login') Login
//-             a.nav__el.nav__el--cta(href='#') Signup

//- //- a.nav__el(href='#') My Booking
//- //-             a.nav__el(href='#' )
//- //-             img.nav__user--image(src='img/user/jpg' alt='User Photo')
//- //-             span Nir